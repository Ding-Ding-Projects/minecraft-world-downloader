{
  "name": "mcwd-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone end-to-end harness: real server + bots through the proxy + world verified on disk.",
  "main": "run.js",
  "scripts": {
    "start": "node run.js",
    "test": "node test/selftest.js"
  },
  "engines": {
    "node": ">=18"
  }
}
