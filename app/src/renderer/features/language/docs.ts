import type { DocArticle } from '../../core/registry';

/**
 * The bundled articles for this feature.
 *
 * They are written for somebody deciding what to set rather than for somebody
 * who already knows: what each control does, what it does not do, where the
 * value is stored, how it fails, and how to check the one claim that would be
 * easy to make and hard to believe — that the humour level never touches a fact.
 */

export const LANGUAGE_DOCS: DocArticle[] = [
  {
    id: 'language.voice',
    title: 'Choosing a language and a voice',
    category: 'Getting started',
    body: [
      'Four settings decide how every label, message and notification in this application reads: the language mode, one humour level for English, one for Cantonese, and whether a dialog carries a decorative emoji. All four live on the **Language and voice** settings section, and all four have a live preview beside them, because the name of a level tells you far less than the level does.',
      '',
      '## The three language modes',
      '',
      '- **English** — every surface in English.',
      '- **Cantonese** — every surface in playful Hong Kong Cantonese.',
      '- **Bilingual** — English as the prominent primary line, with a compact Cantonese line beneath it.',
      '',
      'Bilingual is the mode that stresses the layout, because it is the longest the interface ever gets. The Language destination exists largely so that can be checked rather than assumed: it renders the same screen in all three modes at any width you choose.',
      '',
      '## Two humour levels, and they are independent',
      '',
      'English and Cantonese each carry their own slider from 1 to 5. Level 1 reads fully professional; level 5 is maximum playfulness. They do not move together, so English at 1 beside Cantonese at 5 is a combination you can actually have — and both halves of a bilingual line have to read correctly in it.',
      '',
      '## What the level changes, and what it never changes',
      '',
      'The level styles the **voice**. It never changes the **facts**. Every category is included with no exceptions — information, progress, warnings, errors, security prompts and the confirmation before something is deleted — and at every level a message still names what happened, exactly what it affects, and what your options are.',
      '',
      'That is a strong claim, so this feature checks it rather than asserting it. Each preview sample declares the facts every rung of its ladder must carry: a count, a folder, a host and port, a consequence. The **Voice changes, facts do not** card on the Language destination reads all thirty rendered variants and reports any rung that dropped one. A green line there is a check that ran; a red line names the exact sample, language and level at fault.',
      '',
      '## The disclosure',
      '',
      'Because the level reaches errors and warnings, you are told so before you opt into it. At first run a notification says exactly that and waits — it carries no timer — and the same words stay permanently on the Language destination, so somebody who dismissed the notification a year ago can still read it. Acknowledging it changes nothing except recording that you read it.',
      '',
      '## Emoji',
      '',
      'The emoji switch adds one decorative emoji to a dialog or a message box, and nothing else. A button, an action label, a field label and a screen-reader name never carry one in either position of the switch, because a control name has to be readable aloud. The switch shows both states side by side so you can see the difference before choosing.',
      '',
      '## Where the values live',
      '',
      'The four settings are application-wide and are owned by the shell, under the ids `language.mode`, `language.funny.en`, `language.funny.yue` and `language.emojiInDialogs`. This feature writes those exact ids, so the controls here and the controls elsewhere can never disagree about what is in force.',
      '',
      'A setting id may only be registered once in the whole application, so each control here is registered under a mirror id (`language.voice.mode` and its siblings) that is kept in lockstep with the value **and** its provenance — including being reset when the real setting has never been written, so the provenance line does not present a shipped default as somebody\'s choice. Each control says which pair it is working with, right underneath itself.',
      '',
      '## When the study mode is on',
      '',
      'While the renamable study mode is on, this whole feature behaves as though it were not installed. The settings section is removed rather than disabled, the Language destination is closed, and its command-palette entries disappear with it. Your choices are kept and come back untouched when the mode is switched off; the destination reopens itself only if this feature was the thing that closed it.',
      '',
      '## Resetting',
      '',
      '**Reset the language and voice settings** restores the language mode, both humour levels and the emoji switch to the values this build ships with, and touches nothing else. It asks for confirmation with an ordinary dialog rather than the two-key gate, because it is recoverable: the change is written to local history like any other, so the values you had are readable from there afterwards.'
    ].join('\n'),
    related: ['language.preview', 'core.language', 'core.school']
  },
  {
    id: 'language.preview',
    title: 'The preview matrix and the variant list',
    category: 'Getting started',
    body: [
      'The Language destination answers the comparative question a settings row cannot: what does this look like at the *other* level, in the *other* mode, and does it survive a narrow window.',
      '',
      '## The matrix',
      '',
      'Six cells: three language modes across, both humour extremes down. Each cell is a complete little screen — a heading, a supporting line, the three sample messages and a row of action labels — rendered from the same ladders the application itself reads, at the level that cell is for rather than at the level currently in force.',
      '',
      'Two real controls sit above it:',
      '',
      '- **Preview width** renders every cell at that width in CSS pixels. Bilingual copy is the longest the interface ever gets, so narrowing this is how you check that it wraps instead of clipping.',
      '- **Preview text scale** scales the contents of the previews only, so 125, 150 and 200 per cent can be checked without changing your actual display settings.',
      '',
      'Both are persisted and both are ordinary settings, so they are also reachable from the settings surface and the command palette.',
      '',
      '## Everything in a cell is a static preview',
      '',
      'Nothing inside a preview is focusable and nothing is styled like a live control, including the action labels — they are text, and they are there so the length a bilingual label reaches is visible beside the copy it shares a column with. Every preview carries a caption saying it is a preview.',
      '',
      'The sample messages describe nothing that has happened. No world was saved, nothing is being deleted, and no connection was refused. They are examples written for this surface, and every preview says so.',
      '',
      '## The variant list',
      '',
      'Three samples, two languages, five levels: thirty rows, each showing the exact rendered text. It is the fastest way to compare two levels without dragging a slider back and forth, and it is a normal list, so it carries the normal list behaviour.',
      '',
      '- Multi-select with the checkboxes, **Shift**-click for a range, and **Shift** with the arrow keys for the same range from the keyboard.',
      '- Two separate select-all actions, each naming its own scope: the rows currently shown, or all thirty whether shown or not. A select-all over a filtered list is the one place a count quietly lies about what an action will touch, so this one says which it means.',
      '- Invert and clear.',
      '- The count line separates what is selected from what is shown, and names how much of the selection the current search is hiding.',
      '',
      '### Copy and export',
      '',
      '**Copy** puts the selected text on the clipboard with its category, language and level. **Export** offers every format the application can write and states, before it writes anything, which fields the chosen format cannot carry faithfully.',
      '',
      'There is no delete, rename or move here, and the list says so rather than showing controls that would fail: these rows are shipped preview samples, not your data. The two actions offered are the whole set the list can honestly provide.',
      '',
      '## Failure modes',
      '',
      '- If the clipboard refuses a copy, the notification names the reason and states that nothing was changed. It never reports a copy that did not happen.',
      '- If an export is cancelled at the file dialog, nothing is written and nothing is claimed.',
      '- If a fact check fails, the card turns and names the exact sample, language and level, rather than reporting a total that hides it.',
      '',
      '## Security and privacy',
      '',
      'This surface makes no network request of any kind. The sample text is compiled into the build, the preview never leaves the window, and the only thing written anywhere is your own choice of width, scale and acknowledgement, in the ordinary settings file. Your personal vocabulary, if you have loaded one, is applied to the previews exactly as it is applied everywhere else, and is never included in an export from this surface.',
      '',
      '## Verifying it',
      '',
      '1. Set English to 1 and Cantonese to 5, and confirm the two halves of a bilingual line differ in voice and agree on every fact.',
      '2. Drag **Preview width** to 240 and confirm no cell clips: text wraps, and the page itself never scrolls sideways.',
      '3. Set **Preview text scale** to 200 per cent and confirm the same.',
      '4. Turn the emoji switch off and confirm the emoji leaves the message boxes and was never on the action labels.',
      '5. Select rows with **Shift**-click, search to hide some of them, and confirm the count line names the hidden part of the selection.'
    ].join('\n'),
    related: ['language.voice', 'core.export', 'core.regex']
  }
];
