/** Setting ids and defaults, shared between `index.ts` (which declares the controls) and the panels (which read them). */

export const SEARCH_RADIUS_ID = 'mineflayerInventory.searchRadius';
export const SEARCH_LIMIT_ID = 'mineflayerInventory.searchLimit';
export const AUTO_REFRESH_ID = 'mineflayerInventory.autoRefresh';

export const DEFAULT_SEARCH_RADIUS = 32;
export const DEFAULT_SEARCH_LIMIT = 24;
export const DEFAULT_AUTO_REFRESH = true;

export const POLL_INTERVAL_MS = 1800;
